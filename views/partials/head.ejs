<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="Earn crypto and cash rewards from faucet claims, offerwalls, shortlinks, and micro tasks.">
<title><%= title ? `${title}` : appName %></title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<script src="https://cdn.tailwindcss.com"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        fontFamily: {
          sans: ['Inter', 'ui-sans-serif', 'system-ui']
        },
        colors: {
          brand: {
            ink: '#101318',
            green: '#0F9F6E',
            lime: '#B8E986',
            gold: '#F7B955',
            blue: '#2563EB'
          }
        }
      }
    }
  };
</script>

<style>
  html { scroll-behavior: smooth; }
  .article-body h2 { margin-top: 2rem; margin-bottom: 0.75rem; font-size: 1.5rem; font-weight: 800; color: #101318; }
  .article-body h3 { margin-top: 1.5rem; margin-bottom: 0.5rem; font-size: 1.2rem; font-weight: 700; color: #101318; }
  .article-body p { margin-bottom: 1.1rem; line-height: 1.8; color: #3f4652; }
  .article-body ul { margin: 1rem 0 1rem 1.5rem; list-style: disc; color: #3f4652; }
  .article-body a { color: #0F9F6E; font-weight: 700; }
  .article-body img { margin: 1.5rem 0; border-radius: 0.75rem; }
</style>
