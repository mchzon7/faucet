<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>New Password</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #ECF0F1;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
    }
    .card {
      width: 100%;
      max-width: 400px;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0, 0, 0, 0.15);
    }
    .card-header {
      background-color: #2C3E50;
      color: white;
      font-weight: bold;
      text-align: center;
      padding: 1rem 1.5rem;
    }
    .btn-primary {
      background-color: #3498DB;
      border: none;
    }
    .btn-primary:hover {
      background-color: #2980B9;
    }
  </style>
</head>
<body>

<div class="card">
  <div class="card-header">
    verify account
  </div>
  <div class="card-body">
   <!-- Display flash messages -->
 <% if (success_msg) { %>
    <div style="color: green;">
        <%= success_msg %>
    </div>
    <% } %>
        <% if (error_msg) { %>
            <div style="color: red;">
                <%= error_msg %>
            </div>
            <% } %>
    <form action="/reset-password/<%=token%>" method="post">
      <div class="mb-3">
        <label for="otp" class="form-label">Enter your new password</label>
        <input type="number" class="form-control" name="password" placeholder="enter your new password" required />
      </div>
      <button type="submit" class="btn btn-primary w-100">Submit</button>
    </form>
  </div>
</div>
</body>
</html>