<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Visit Short Links - Faucet Platform</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      background-color: #ECF0F1;
    }
    .navbar {
      background-color: #2C3E50;
    }
    .navbar .nav-link, .navbar-brand {
      color: #ECF0F1 !important;
    }
    .links-card {
      background-color: #fff;
      border: 1px solid #BDC3C7;
      border-radius: 12px;
      padding: 30px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }
    .btn-primary {
      background-color: #3498DB;
      border: none;
    }
    .btn-primary:hover {
      background-color: #2980B9;
    }
    .pagination{
      margin-top: 20px;
    }

    .pagination a,
    .pagination span{
      margin: 0 5px;
      text-decoration: none;
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">CRASHCASH</a>
      <button class="navbar-toggler text-white" type="button" data-bs-toggle="collapse"
              data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon bg-white"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/dashboard">Dashboard</a></li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="faucetDropdown" role="button" data-bs-toggle="dropdown">
              Faucet List
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/math">Solve-task</a></li>
              <li><a class="dropdown-item" href="/typing-captcha">Type-task</a></li>
              <li><a class="dropdown-item" href="/ptc3">V-ShortLinks</a></li>
              <li><a class="dropdown-item" href="/page1">V-pages</a></li>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="faucetDropdown" role="button" data-bs-toggle="dropdown">
              Offerwalls
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/task">OOFD</a></li>
              <li><a class="dropdown-item" href="/complete-task">Daily-Task</a></li>
            </ul>
          </li>
          <li class="nav-item"><a class="nav-link" href="/referral">Referrals</a></li>
          <li class="nav-item"><a class="nav-link" href="/withdrawal">Withdraw</a></li>
          <li class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
          <li class="nav-item"><a class="nav-link" href="/contact">Contact</a></li>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
</nav>

<iframe src="https://ad2bitcoin.com/ad.php?ref=Chizon7&width=728" marginwidth="0" marginheight="0" width="320" height="50" scrolling="no" border="0" frameborder="0"></iframe>
<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="links-card shadow-sm">
        <h4 class="mb-4 text-center">Links-Reward</h4>
        <p class="text-muted text-center">Earn rewards by visiting the following short links:</p>
        <div class="card-body text-center">
          <h6>Your Balance: <span id="balance" class="fw-bold text-success" ><p><%= user.balance.toFixed(4) %> CRC-Coin</p></span></h6>
      </div>
      <div class="mt-3 text-center">
        <h6>Available Ads:</h6>
      </div>
        <div class="list-group">

            <% ptcLinks.forEach(ptcLinks => { %>

              <li class="list-group-item d-flex justify-content-between align-items-center">
                <h3><span><%= ptcLinks.name %></span></h3>
                <p><%= ptcLinks.description %></p>
                <button class="visit-link btn btn-primary btn-sm" data-url="<%= encodeURIComponent(ptcLinks.url) %>" data-id="<%= ptcLinks._id %>">
                  Visit Link
                </button>
                <p class="countdown" id="timer-<%= ptcLinks._id %>" style="display: none;"></p>
            </li>
            <% }) %>
        </div>
        <!-- Pagination Controls -->
<div class="pagination">
  <% if (currentPage> 1) { %>
      <a href="/ptc3?page=<%= currentPage - 1 %>">Previous</a>
      <% } %>

          <% for (let i=1; i <=totalPages; i++) { %>
              <% if (i===currentPage) { %>
                  <span><strong>
                          <%= i %>
                      </strong></span>
                  <% } else { %>
                      <a href="/ptc3?page=<%= i %>">
                          <%= i %>
                      </a>
                      <% } %>
                          <% } %>

                              <% if (currentPage < totalPages) { %>
                                  <a href="/ptc3?page=<%= currentPage + 2 %>">Next</a>
                                  <% } %>
</div>
        <div class="mt-3 text-center">
          <p class="text-muted">Please complete the short link visits to claim your rewards!</p>
        </div>
      </div>
    </div>
  </div>
</div>
<script type="text/javascript">
 atOptions = {
  'key' : '0491ef92a4c0f3050c9ab079df401f08',
  'format' : 'iframe',
  'height' : 50,
  'width' : 320,
  'params' : {}
 };
</script>
<script type="text/javascript" src="//www.highperformanceformat.com/0491ef92a4c0f3050c9ab079df401f08/invoke.js"></script>
<iframe src="https://ad2bitcoin.com/ad.php?ref=Chizon7&width=728" marginwidth="0" marginheight="0" width="400" height="40" scrolling="no" border="0" frameborder="0"></iframe>


<script>
  document.addEventListener("DOMContentLoaded", function () {
    document.querySelectorAll(".visit-link").forEach(button => {
      button.addEventListener("click", function () {
        const url = this.getAttribute("data-url");
        const linkId = this.getAttribute("data-id");
        const timerElement = document.getElementById(`timer-${linkId}`);
        const originalTitle = document.title;
  
        let timeLeft = 10;
        let stayedFullTime = false;
        let tabClosed = false;
  
        // Show timer and start countdown
        timerElement.style.display = "block";
        timerElement.innerText = `Time remaining: ${timeLeft} seconds`;
  
        // Open new tab for tracking
        const newTab = window.open(`/track-link?url=${url}&linkId=${linkId}, "_blank"`);
  
        if (!newTab) {
          alert("Please allow pop-ups to continue.");
          return;
        }
  
        // Start the countdown
        const countdown = setInterval(() => {
          if (tabClosed) {
            clearInterval(countdown);
            return;
          }
  
          timeLeft--;
          timerElement.innerText = `Time remaining: ${timeLeft} seconds`;
          document.title = `⏳ ${timeLeft}s left - PTC`;
  
          if (timeLeft <= 0) {
            clearInterval(countdown);
            stayedFullTime = true;
            document.title = originalTitle; // Restore title
  
            // Reward user if they stayed for full time
            fetch("/reward-user", {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({ linkId: linkId, visitedUrl: url })
            })
            .then(response => response.json())
            .then(data => {
              if (data.success) {
                alert("You have been rewarded!");
                window.location.reload(); // Refresh balance
              } else {
                alert("Error: " + data.message);
              }
            });
          }
        }, 1000);
  
        // Detect if the user closes the new tab
        const checkTab = setInterval(() => {
          if (newTab.closed) {
            clearInterval(checkTab);
            clearInterval(countdown);
            tabClosed = true;
            document.title = originalTitle;
            timerElement.innerText = "You closed the link too early. No reward given.";
          }
        }, 500);
      });
    });
  });
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>